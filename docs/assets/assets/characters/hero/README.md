# hero (player)

Drop: `idle.png`, `walk.png`, `attack.png`.
Sheet grid = [frames] columns x [dirs] rows.
Config: `kHeroCfg` in `lib/main.dart` (dirs = 16).
See [../../README.md](../README.md) for the full guide.

# objects

Single images: `barrel.png`, `coins.png`, `potions.png`.
(`potions.png` is a row of icons: 1st = health, 2nd = mana.)
Paths: `kBarrelPath` / `kCoinsPath` / `kPotionsPath` in `lib/main.dart`.
